echo "🔧 Generating GraphQL types..."
pnpm codegen || exit 1

echo "🔍 Running TypeScript type check..."
pnpm type-check || exit 1

echo "✨ Running ESLint (warnings treated as errors)..."
pnpm lint --max-warnings 0 || exit 1

echo "✅ All pre-commit checks passed!"
